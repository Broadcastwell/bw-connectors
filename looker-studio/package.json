{
  "name": "broadcastwell-looker-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Broadcastwell Looker Studio community connector (Apps Script). Built from src/ and packages/api.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check && node ../packages/api/scripts/check-copy.mjs ../looker-studio",
    "test": "node --test test/*.test.mjs",
    "demo": "node scripts/live-demo.mjs",
    "fixtures": "node scripts/capture-fixtures.mjs"
  }
}
